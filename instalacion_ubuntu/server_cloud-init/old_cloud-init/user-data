#cloud-config
autoinstall:
  version: 1
  identity:
    hostname: ubuntu-server
    username: ubuntu
    password: password123 # Reemplaza con un hash de tu contraseña
  ssh:
    install-server: yes
  keyboard:
    layout: es
  locale: en_ES
  network:
    version: 2
    ethernets:
      ens3:
        dhcp4: true
  storage:
    layout:
      name: direct
  # Evitar la confirmación manual
  early-commands:
  #  - "echo 'autoinstall' > /target/var/lib/cloud/seed/nocloud-net/meta-data"
  #  - openssh-server
  user-data:
    late-commands:
      - curtin in-target --target=/target /bin/bash -c "sed -i 's/^Prompt=.*$/Prompt=never/' /etc/apt/apt.conf.d/00aptitude"
